/**
 * ProxyTrap: unique symbol to handle recursive proxies.
 */
export const ProxyTrap: unique symbol = Symbol('ProxtTrap');

/**
 * Contract: unique symbol to access subject-policy interface.
 */
export const Contract: unique symbol = Symbol('Contract');
